{% extends 'base.html.twig' %}
{% block body %}

    <div class="links">
        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('admin_home') }}">Admin Dashboard</a>
        {% endif %}
        {% if is_granted('ROLE_USER') %}
            <a href="{{ path('user_home') }}">Transaction History</a>
            <a href="{{ path('user_home') }}">Menu</a>
            <a href="{{ path('user_myaccount') }}">My Account</a>
        {% endif %}
    </div>
{% endblock %}